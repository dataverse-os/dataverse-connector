export enum IndexFileContentType {
  NFT = "NFT",
  CID = "CID",
  MIRROR = "MIRROR",
  VIDEO = "VIDEO",
  WEBSITE = "WEBSITE",
}

export enum OriginType {
  curate,
  upload,
  follow,
  click,
}

export enum FileType {
  Public,
  Private,
  Datatoken,
}
