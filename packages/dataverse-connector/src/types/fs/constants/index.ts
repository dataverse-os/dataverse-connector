export enum FolderType {
  PrivateFolderType,
  UnionFolderType,
}

export enum FileType {
  PublicFileType,
  PrivateFileType,
  PayableFileType,
}

export const TEMP = "temp";

export const fsVersion = "0.11";
