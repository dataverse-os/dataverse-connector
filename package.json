{
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "lerna run build",
    "build:core-connector": "lerna run build --scope=@dataverse/core-connector",
    "test": "pnpm --filter 'demo' dev",
    "publish": "lerna publish --no-private"
  },
  "dependencies": {
    "lerna": "^7.0.1",
    "ethers": "5.6.9"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^5.1.6"
  }
}
